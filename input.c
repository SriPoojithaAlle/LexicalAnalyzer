#include <stdio.h>

int main()
{
    int sum = 10 + 20;
    float average = sum / 2.0;

    // Calculate average

    if (average > 15)
    {
        printf("Pass");
    }

    return 0;
}
